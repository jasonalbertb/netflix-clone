const DASHBOARD = "/";
const BROWSE = "/browse";
const SHOW_PAGE = `${BROWSE}/:type/:id`;
const CHOOSE_PROFILE = "/choose-profile"
const SIGNIN = "/sign-in";
const SIGNUP = "/sign-up";
const ERR = "/error";
const ADD_PROFILE = "/add-profile"
export const ROUTES = {
    DASHBOARD,
    BROWSE,
    SHOW_PAGE,
    CHOOSE_PROFILE,
    ADD_PROFILE,
    SIGNIN,
    SIGNUP,
    ERR
}
